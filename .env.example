# =============================================================================
# Even/Odd League System - Environment Configuration Template
# =============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Update values suitable for your local environment.
# 3. NEVER commit the actual .env file to version control.
#
# SECURITY NOTE:
# - Secrets (like AUTH_TOKENS) should not be hardcoded in code.
# - In production, use a secrets manager or secure environment injection.
#
# =============================================================================

# --- General System Settings ---
# Log level for agents (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Base host for all agents (usually localhost for dev)
BASE_HOST=localhost

# League identifier (optional override)
LEAGUE_ID=league_2025_even_odd

# --- Network Ports (must align with SHARED/config/system.json) ---
LEAGUE_MANAGER_PORT=8000
REFEREE_PORT_START=8001
REFEREE_PORT_END=8002
PLAYER_PORT_START=8101
PLAYER_PORT_END=9100

# --- Security & Auth ---
# Minimum length for generated authentication tokens
AUTH_TOKEN_LENGTH=32

# Secret key for internal signing (if applicable in future extensions)
# SECRET_KEY=change_this_to_a_secure_random_string

# --- Resilience & Performance ---
# Retry policy (matches defaults in system.json)
RETRY_MAX_RETRIES=3
RETRY_INITIAL_DELAY_SEC=2.0
RETRY_MAX_DELAY_SEC=10.0

# Timeouts (seconds)
TIMEOUT_REGISTRATION=10
TIMEOUT_GAME_JOIN_ACK=5
TIMEOUT_PARITY_CHOICE=30
TIMEOUT_GAME_OVER=5
TIMEOUT_MATCH_RESULT=10
TIMEOUT_LEAGUE_QUERY=10
TIMEOUT_GENERIC=10

# Global timeout for HTTP requests (seconds)
REQUEST_TIMEOUT_SEC=30

# --- Data & Logs (optional overrides) ---
DATA_DIR=SHARED/data
LOG_DIR=SHARED/logs
